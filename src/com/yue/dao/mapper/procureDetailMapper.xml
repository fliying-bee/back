<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC   
    "-//mybatis.org//DTD Mapper 3.0//EN"  
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
 <mapper namespace="com.yue.dao.ProcureDetailDao">  
    <!-- 插入 -->
    <insert id="insertProcureDetail" parameterType="com.yue.model.ProcureDetail">
        insert into BDProcureDetail(procureId,proDetailId,procureDetailType,proId,procureDetailCount) 
               values(#{procureId},#{proDetailId},#{procureDetailType},#{proId},#{procureDetailCount})
    </insert>  
 </mapper>